# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-O2 -march=znver3 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

MAKEOPTS="-j8"

GENTOO_MIRRORS="https://mirrors.aliyun.com/gentoo"

CPU_FLAGS_X86="aes avx avx2 f16c fma3 mmx mmxext pclmul popcnt rdrand sha sse sse2 sse3 sse4_1 sse4_2 sse4a ssse3"

INPUT_DEVICES="libinput synaptics"

VIDEO_CARDS="amdgpu radeonsi"

ACCEPT_LICENSE="*"

ACCEPT_KEYWORDS="~amd64"

GRUB_PLATFORMS="efi-64"

L10N="en en-US"

RUBY_TARGETS="ruby25 ruby26 ruby30"

USE="X aac aspell bash-completion boost branding caps contrib cpudetection \
     dbus evdev exif experimental filecaps flac freetype fuse gd gles2 gpm \
     gstreamer gzip hardened hddtemp highlight imagemagick int64 \
     introspection jemalloc jpeg jpeg2k json kms libav-aac libnotify \
     lm-sensors lz4 lzma lzo mime mp3 mp4 mpeg nano-syntax networkmanager \
     nls nss numa ogg opencl opencv opengl osc pam pango pcap pdf pie png \
     pulseaudio python recode samba savedconfig screencast sdl seccomp \
     simplexml slang smp sockets sound spell ssh startup-notification svg \
     symlink systemd theora timezone truetype udev udisks upnp upnp-av \
     upower usb video vim vorbis wavpack wayland wayland-compositor wifi \
     xcomposite xfs xft xinerama xkb xml xmlrpc xorg xpm xrandr xwayland xz \
     zeroconf zsh-completion -bindist -harfbuzz -kde -qt5 -static"


